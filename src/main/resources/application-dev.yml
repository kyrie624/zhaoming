# 本地开发环境配置
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/zhaoming?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: zhaoming
    password: zhaoming123
    # 连接池（Spring Boot 默认 HikariCP）
    hikari:
      minimum-idle: 2
      maximum-pool-size: 10
      connection-timeout: 30000

# 开发期在控制台打印 SQL，便于调试
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
